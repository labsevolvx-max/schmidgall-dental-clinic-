import { Case } from "@/components/ui/cases-with-infinite-scroll";

const Testimonials = () => {
    return (
        <section id="testimonials" className="bg-[#0f172a] relative overflow-hidden">
            <Case />
        </section>
    );
};

export default Testimonials;
